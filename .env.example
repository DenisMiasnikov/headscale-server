# Docker Hub username used for pulling images
DOCKER_USERNAME=your-docker-username

# Your public domain
DOMAIN=your-domain.com

# Server URL (should match DOMAIN)
SERVER_URL=https://your-domain.com

# MagicDNS base domain
BASE_DOMAIN=tail.your-domain.com

# Default headscale namespace
DEFAULT_USER=default

# UI admin credentials
UI_USERNAME=admin
UI_PASSWORD=change_me

# Session secret (generate with: openssl rand -hex 32)
SESSION_SECRET=change_me

# Whether cookies should be secure (true for HTTPS)
COOKIE_SECURE=true

# Email for Let's Encrypt
LETSENCRYPT_EMAIL=your-email@example.com
